import React from 'react';

const Feature = ({feature}) => {
    // console.log(feature)
    return (
        <div>
            <span>{feature}</span>
        </div>
    );
};

export default Feature;